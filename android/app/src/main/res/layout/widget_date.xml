<?xml version="1.0" encoding="utf-8"?>
<!-- Fully transparent: no background is set on the root, so only the text shows.
     White text + a symmetric dark-gray shadow "halo" keeps it readable on both
     light and dark wallpapers. autoSize* lets the text scale as the user resizes
     the widget (API 26+; below that the textSize fallback is used). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@android:color/transparent"
    android:padding="4dp"
    tools:ignore="UnusedAttribute">

    <TextView
        android:id="@+id/tv_et_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#FFFFFFFF"
        android:textStyle="bold"
        android:shadowColor="#CC2B2B2B"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="3"
        android:textSize="22sp"
        android:autoSizeTextType="uniform"
        android:autoSizeMinTextSize="14sp"
        android:autoSizeMaxTextSize="44sp"
        android:autoSizeStepGranularity="1sp" />

    <TextView
        android:id="@+id/tv_gc_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#FFFFFFFF"
        android:shadowColor="#CC2B2B2B"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="3"
        android:textSize="13sp"
        android:autoSizeTextType="uniform"
        android:autoSizeMinTextSize="10sp"
        android:autoSizeMaxTextSize="22sp"
        android:autoSizeStepGranularity="1sp" />
</LinearLayout>
