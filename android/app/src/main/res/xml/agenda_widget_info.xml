<?xml version="1.0" encoding="utf-8"?>
<!-- Agenda widget: larger, freely resizable. updatePeriodMillis=0 because the
     app drives updates (resume / event change / midnight). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="250dp"
    android:minHeight="180dp"
    android:minResizeWidth="180dp"
    android:minResizeHeight="110dp"
    android:maxResizeWidth="360dp"
    android:maxResizeHeight="360dp"
    android:targetCellWidth="4"
    android:targetCellHeight="3"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/widget_agenda"
    android:previewLayout="@layout/widget_agenda"
    android:updatePeriodMillis="0" />
